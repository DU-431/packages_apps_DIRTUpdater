<?xml version="1.0" encoding="utf-8"?>
 <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:id="@+id/widgetlayout">

	<ImageView
	    android:id="@+id/blankImage"
	    android:layout_width="fill_parent"
	    android:layout_height="75dp"
	    android:contentDescription="@string/desc"
	    android:src="@drawable/menubackground"
	    android:layout_centerInParent="true"
	    android:scaleType="fitXY" />
	
	<TextView
        android:id="@+id/myImageViewText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/blankImage"
        android:layout_alignTop="@+id/blankImage"
        android:layout_alignRight="@+id/blankImage"
        android:layout_alignBottom="@+id/blankImage"
        android:layout_margin="1dp"
        android:gravity="center"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="#0099cc" 
        />
	<LinearLayout
    	android:layout_width="fill_parent"
    	android:layout_height="wrap_content"
    	android:id="@+id/underBar"
    	android:layout_below="@+id/blankImage">
		<ImageView
		    android:id="@+id/line"
		    android:layout_width="match_parent"
		    android:layout_height="5dp"
		    android:contentDescription="@string/desc"
		    android:src="@drawable/linedark" 
		    />
	</LinearLayout>
</RelativeLayout>